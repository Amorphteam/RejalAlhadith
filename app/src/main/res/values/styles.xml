<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:typeface">monospace</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/page_background</item>
        <item name="android:colorBackground">@color/surface</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="android:textColor">@color/text_primary</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="ToolbarTitle" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:typeface">monospace</item>
        <item name="android:fontFamily">monospace</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <!--style theme-->
    <style name="StyleMenuStyle" parent="Theme.AppCompat.DayNight.Dialog">
        <item name="android:windowAnimationStyle">@style/StyleDialogAnimation</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:textViewStyle">@style/BottomShadowStyle</item>
    </style>

    <style name="StyleDialogAnimation">
        <item name="android:windowEnterAnimation">@anim/scale_from_corner</item>
        <item name="android:windowExitAnimation">@anim/scale_from_corner_reverse</item>
    </style>

    <style name="BottomShadowStyle" parent="android:Widget.TextView">
        <item name="android:shadowColor">@color/shadow_color</item>
        <item name="android:shadowRadius">4</item>
        <item name="android:shadowDy">2</item>
    </style>


</resources>
